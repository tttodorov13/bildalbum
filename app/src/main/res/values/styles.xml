<resources>

    <style name="BuildAlbumTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionBarStyle">@style/BuildAlbumActionBar</item>
    </style>

    <style name="BuildAlbumActionBar" parent="Widget.AppCompat.ActionBar.Solid">
        <!-- Used for the background -->
        <item name="android:background">@color/colorPrimary</item>
        <!-- Used for the title -->
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="BuildAlbumAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the background -->
        <item name="android:background">@color/colorPrimary</item>
        <!-- Used for the title -->
        <item name="android:textColor">@color/colorWhite</item>
        <!-- Used for the items -->
        <item name="textColorAlertDialogListItem">@color/colorWhite</item>
    </style>

</resources>
